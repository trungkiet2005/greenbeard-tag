__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/

# LaTeX build artefacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.spl
paper/*.run.xml

results/figure_log*.txt

# CSF build artefacts (paper-csf/ is assembled by paper-csf/assemble.py)
paper-csf/*.aux
paper-csf/*.bbl
paper-csf/*.blg
paper-csf/*.log
paper-csf/*.out
paper-csf/*.toc
paper-csf/*.abs
paper-csf/*.fls
paper-csf/*.fdb_latexmk
paper-csf/*.synctex.gz
paper-csf/*.spl
paper-csf/*.run.xml

# Publisher template packages, downloaded not authored
els-cas-templates.zip

# Submission apparatus: the replication repository is cited in the Data
# availability statement, so anything an editor or referee should not read
# over our shoulder stays out of it.  The built PDFs are already covered above.
paper-csf/cover_letter.md
paper-csf/SUBMISSION_CHECKLIST.md

# The typeset manuscript is not published here.  The journals are told that no
# part of the paper has appeared before, and a downloadable PDF in a public
# repository is the one thing that could be read against that, so the built
# manuscript stays local while the code, data and figures that reproduce it
# stay public.  Remove these lines once the paper is accepted.
paper/main.pdf
paper-*/main.pdf
