%% CSF-only addition to the Model section.
%%
%% The flow, the transversal eigenvalue and the edge transcritical bifurcation
%% used to live here, because the venue-neutral master described the dynamics
%% in prose and never wrote the replicator equation down.  They now live in the
%% master, where every venue version needs them and where the appendix proofs
%% reference them.  What remains here is the one sentence that is specific to
%% this package: the CSF Results section opens with a bifurcation inventory
%% table that the master does not carry.
Table~\ref{tab:bifurcations} in Section~\ref{sec:results} collects the
qualitative transitions below with their control parameters and locations, so
that the local edge bifurcations and the global basin properties can be read
apart at a glance.
